# Docker Compose Override for Production
# Generated by Ansible

services:
  execution:
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 28G
        reservations:
          memory: 24G

  consensus:
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G

  rpc:
    restart: unless-stopped
    ports:
      - "{{ monad_rpc_port }}:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
