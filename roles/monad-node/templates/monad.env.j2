# Monad Environment Configuration
# Generated by Ansible

CHAIN={{ env }}
MONAD_HOME="{{ monad_home }}"
MONAD_DATA_DIR="{{ monad_data_dir }}"
MONAD_CONFIG_DIR="{{ monad_config_dir }}"
MONAD_CHAIN_ID="{{ monad_chain_id }}"
MONAD_RPC_PORT="{{ monad_rpc_port }}"
MONAD_P2P_PORT="{{ monad_p2p_port }}"
MONAD_LOG_LEVEL="info"
MONAD_LOGS_DIR="{{ monad_logs_dir }}"
REMOTE_VALIDATORS_URL="{{ monad_validators_config_url }}"
REMOTE_FORKPOINT_URL="{{ monad_forkpoint_config_url }}"
RUST_LOG="debug,h2=warn,tower=warn,opentelemetry_sdk=warn,opentelemetry-otlp=warn"

RETENTION_LEDGER={{ monad_retention_ledger | default(600) }}
RETENTION_WAL={{ monad_retention_wal | default(300) }}
RETENTION_FORKPOINT={{ monad_retention_forkpoint | default(300) }}
RETENTION_VALIDATORS={{ monad_retention_validators | default(43200) }}
