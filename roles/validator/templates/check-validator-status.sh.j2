#!/bin/bash
# Check Monad Validator Status
# Generated by Ansible

set -e

source {{ monad_home }}/staking-sdk-cli/cli-venv/bin/activate
cd {{ monad_home }}/staking-sdk-cli

echo "============================================"
echo "Monad Validator Status"
echo "============================================"

# Check validator set
echo ""
echo "Checking validator set..."
python staking-cli/main.py query validator-set --type execution --config-path config.toml

# If validator ID is known, query specific info
# python staking-cli/main.py query validator --validator-id YOUR_ID --config-path config.toml

echo ""
echo "============================================"
