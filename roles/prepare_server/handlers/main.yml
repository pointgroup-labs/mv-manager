---
- name: Start set-hugepages service
  ansible.builtin.systemd:
    name: set-hugepages
    state: started
    enabled: yes
    daemon_reload: yes

- name: Reload udev rules
  ansible.builtin.command: udevadm control --reload-rules

- name: Trigger udev rules
  ansible.builtin.shell: udevadm trigger && udevadm settle

- name: Reload UFW
  community.general.ufw:
    state: reloaded
